# Version 0.1.1

- Can find polynomial splines using b-splines basis. Note: apart from the
degree of the piecewise polynomial b-splines, the default behaviour for `bs` is
assumed.

- Added ability to supply a custom baseline scenario to `plte_builder via
the `cf` argument.